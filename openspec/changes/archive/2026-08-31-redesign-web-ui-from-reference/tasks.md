## 1. Visual Foundation and Shell

- [x] 1.1 Implement **Shared visual tokens and surface primitives** so **The web interface uses a shared warm visual system** through semantic light/dark CSS variables, Tailwind/HeroUI mappings, `PageScaffold`, and `SurfaceCard`; verify computed token roles in both themes, forwarded DOM attributes on both primitives, and `pnpm test:web`.
- [x] 1.2 Implement the **Mobile-first centered app shell** so **The application provides a shared app shell** and **Pages use a mobile-first centered content frame** with 16px/24px gutters, a 768px maximum, safe-area-aware header, floating five-item navigation, 44px targets, selected-route state, and unobstructed final content; verify focused layout assertions in `apps/web/specs/homeTransactions.spec.tsx` and a 390px/768px manual browser check.
- [x] 1.3 Prepare the two **Layered transaction banner assets** by exporting supplied image 2 to `apps/web/public/images/ui/duoji-banner-background.webp` and supplied image 3 with its black background removed to `apps/web/public/images/ui/duoji-banner-travel.webp`; verify transparent edges at 1x and 2x, correct source-image identity, successful static loading, and a maximum size of 350 KB per asset.
- [x] 1.4 Implement `TransactionHero` so **The transaction page uses the approved layered banner** with live title/actions, 220px-to-300px responsive height, narrow-screen text-safe positioning, dark-theme scrim, and missing-asset fallback; verify component assertions for visible/operable controls at 320px and failed image requests.

## 2. Transaction Page Family

- [x] 2.1 [P] Apply **Page-family migration without business-logic changes** to the transaction page, `TransactionCalendar`, `WeekStrip`, and `MonthGrid` so the hero, calendar surface, selected date, display-mode toggle, and week/month gestures match the approved visual hierarchy while existing range queries and selections remain identical; verify the calendar and transaction-page cases in `apps/web/specs/homeTransactions.spec.tsx`.
- [x] 2.2 [P] Make **Shared surfaces present content and application states consistently** in `TransactionList`, `TransactionModal`, all-books mode, loading, empty, error, and missing-book views so cards, chips, amounts, participant rows, disabled actions, and recovery messages use the shared surface contract without changing mutations; verify existing create/edit/delete, aggregate-mode tooltip, and state assertions with `pnpm test:web`.

## 3. Analysis and Settlement Page Families

- [x] 3.1 [P] Apply **Page-family migration without business-logic changes** to the report page frame, time/member/book/tag filters, summary cards, and empty state so they use compact headings and shared surfaces while emitting the same filter state and aggregates; verify `apps/web/specs/timeRangeSelector.spec.tsx`, `apps/web/specs/reportSection.spec.tsx`, and `apps/web/specs/reportAggregate.spec.ts`.
- [x] 3.2 [P] Restyle report charts, monthly trends, and category breakdown/detail surfaces with semantic light/dark chart colors and the shared spacing hierarchy while preserving data series, labels, and category navigation; verify existing chart-adjacent component tests plus a light/dark report render with fixed fixture data.
- [x] 3.3 [P] Migrate settlement list, unsettled transaction, and record-detail views to compact headings and shared cards while preserving balances, notification states, record navigation, and transfer completion semantics; verify `apps/web/specs/settlementPage.spec.tsx` and `apps/web/specs/settlementRecordDetailPage.spec.tsx`.
- [x] 3.4 [P] Restyle settlement confirmation, transfer, and Markdown modals with the common form/modal surface, visible focus, and long-content overflow while preserving confirm/cancel callbacks and generated settlement text; verify their existing modal assertions and keyboard close behavior with `pnpm test:web`.

## 4. Configuration, Authentication, and Onboarding

- [x] 4.1 [P] Apply **Page-family migration without business-logic changes** to global settings and account-book create/edit pages so compact headings, grouped surfaces, theme controls, currency fields, and destructive actions use the new system without changing navigation or saved values; verify `apps/web/specs/accountBookSettings.spec.tsx` and light/dark theme persistence.
- [x] 4.2 [P] Restyle category/member management and reusable form/modal controls so category icons, subcategories, member rows, field errors, confirm dialogs, and action hierarchy remain readable at 320px while preserving repository and store calls; verify the existing category and account-book settings test suites.
- [x] 4.3 [P] Restyle login and onboarding shells, welcome modal, tutorials, and highlighted anchors so the warm visual system is continuous before and after account-book entry while preserving step order, skip/back behavior, and anchor click-through; verify `apps/web-e2e/src/onboarding.spec.ts` and focused onboarding component tests.

## 5. Cross-Cutting Verification

- [x] 5.1 Enforce **Theme, state, localization, and accessibility contracts** so **The redesigned interface remains accessible** in light/dark themes, English/Traditional Chinese, loading/empty/error/not-found states, long labels, keyboard navigation, 44px touch targets, 4.5:1 normal-text contrast, 3:1 large-text/non-text contrast, and reduced motion; verify a route-family accessibility checklist plus focused DOM assertions for focus, overflow, and `prefers-reduced-motion: reduce`.
- [x] 5.2 Add **Deterministic responsive and visual verification** proving **Existing application workflows remain behaviorally compatible** through `apps/web-e2e/src/ui-visual-regression.spec.ts` snapshots and DOM assertions at 390x844 and 768x1024 for transaction, report, settlement, settings, login, and onboarding in both themes, including banner-asset failure and unobstructed final content; verify `pnpm test:web`, `pnpm build:web`, and `pnpm nx e2e web-e2e -- --project=chromium`, then manually compare the transaction page with the approved reference.

## 6. Compact Typography and Icon Pass

- [x] 6.1 Implement **One-step compact typography and icon scale** foundation so **The interface uses a one-step compact typography and icon scale** through updated `apps/web/DESIGN.md`, shared typography roles, shell branding, account-book selector, navigation glyphs, common form styles, and the `TransactionHero` 28px-to-48px display range while preserving existing spacing and 44px hit areas; verify focused assertions in `apps/web/specs/visualPrimitives.spec.tsx`, `apps/web/specs/transactionHero.spec.tsx`, and `apps/web/specs/homeTransactions.spec.tsx`.
- [x] 6.2 [P] Apply the compact semantic scale to the transaction family, including calendar, transaction lists, states, and transaction modals, so body and primary-control text remain at least 14px, metadata remains at least 12px, and icon glyphs reduce one step without changing callbacks or control geometry; verify transaction and calendar presentation suites plus `apps/web/specs/homeTransactions.spec.tsx`.
- [x] 6.3 [P] Apply the compact semantic scale to report and settlement families, including filters, summaries, charts, lists, record details, empty states, and modals, so headings, amounts, labels, and glyphs retain their information priority without changing calculations or navigation; verify report/settlement component suites and fixed light/dark chart fixtures.
- [x] 6.4 [P] Apply the compact semantic scale to settings, account-book, category/member, login, and onboarding families so headings, form text, metadata, tutorials, and glyphs become one step smaller while long English and Traditional Chinese content remains readable at 320px; verify account-book, category/member, and onboarding component suites plus `apps/web-e2e/src/onboarding.spec.ts`.
- [x] 6.5 Refresh deterministic compact visual verification at 390x844 and 768x1024 in both themes, assert unchanged 44px targets, banner fallback, selected navigation, and unobstructed final content, then manually compare the compact transaction page with the approved reference; verify `pnpm test:web`, `pnpm build:web`, and `pnpm nx e2e web-e2e -- --project=chromium`.

## 7. Transaction Hero and Calendar Overlap

- [x] 7.1 Implement **Normal-flow transaction hero/calendar overlap** so **The transaction page uses the approved layered banner** with a page-local Hero/calendar wrapper, 28px/44px responsive negative overlap, 8px/16px calendar inset, foreground stacking, and matching Hero bottom-safe padding while preserving the existing Hero height, live controls, and calendar behavior; verify focused assertions in `apps/web/specs/transactionHero.spec.tsx` and `apps/web/specs/homeTransactions.spec.tsx`.
- [x] 7.2 Extend **Deterministic responsive and visual verification** so transaction layouts at 320px, 390x844, and 768x1024 prove the calendar crosses the Hero boundary, Hero actions remain above it, and expanded month content stays above the transaction list; verify `pnpm test:web`, `pnpm build:web`, and `pnpm nx e2e web-e2e -- --project=chromium`, then manually compare refreshed transaction baselines with the approved reference.

## 8. Report Chart Layout Fix

- [x] 8.1 Implement **Compact full-height report chart plotting areas** so **Report charts use compact full-height plotting areas** through a height-filling `ReportApexChart` wrapper and compact responsive category-donut and monthly-trend containers, while preserving series, semantic colors, category-list topology, text priority, and 44px actions; verify focused assertions in `apps/web/specs/reportChartsPresentation.spec.tsx` and `apps/web/specs/reportCategoryBreakdown.spec.tsx` using fixed one-month and multi-category fixtures.
- [x] 8.2 Extend **Deterministic responsive and visual verification** so populated report layouts at 390x844 and 768x1024 in light and dark themes prove the excessive chart whitespace is removed without clipping totals, labels, legends, axes, bars, category text, amounts, or actions; verify `pnpm test:web`, `pnpm build:web`, and the report cases in `pnpm nx e2e web-e2e -- --project=chromium`, then manually compare the refreshed report baselines with the approved screenshot and `docs/superpowers/specs/2026-08-29-report-chart-layout-fix-design.md`.

## 9. Calendar Alignment and Minimum Touch Target

- [x] 9.1 Implement **Aligned transaction calendar and history surfaces** and **Twenty-four-pixel minimum touch targets** so **The transaction page uses the approved layered banner**, **The redesigned interface remains accessible**, and **The interface uses a one-step compact typography and icon scale** with equal calendar/history horizontal boundaries, the page scaffold's 16px gap as the only vertical separation before every transaction-history state, a 24 by 24 CSS pixel calendar display-mode button and either glyph state, and a site-wide 24 by 24 CSS pixel minimum that leaves existing larger controls unchanged; keep `SurfaceCard` spacing-neutral and preserve explicit spacing for `TransactionList` consumers outside `PageScaffold`; verify focused assertions in `apps/web/specs/homeTransactions.spec.tsx`, `apps/web/specs/transactionSurfacePresentation.spec.tsx`, `apps/web/specs/transactionCalendarPresentation.spec.tsx`, and the updated `apps/web/DESIGN.md` contract.
- [x] 9.2 Extend **Deterministic responsive and visual verification** so transaction layouts at 390x844 and 768x1024 prove equal calendar/history bounding boxes, a 16px calendar-to-history gap with no accumulated list margin, the preserved 28px/44px Hero overlap, an operable 24px display-mode control, and unchanged larger controls in light and dark themes; verify `pnpm test:web`, `pnpm build:web`, and the transaction cases in `pnpm nx e2e web-e2e -- --project=chromium`, then manually compare refreshed transaction baselines with the approved screenshot and `docs/superpowers/specs/2026-08-30-calendar-width-and-touch-target-design.md`.

## 10. Transaction Hero Height

- [x] 10.1 Implement **Fixed 220px transaction hero height** so **The transaction page uses the approved layered banner** at a 220px block height from 320px through wider centered layouts, exposing more downstream content on desktop while preserving live title/actions, responsive bottom-safe padding, asset fallback, dark-theme treatment, and the 28px/44px normal-flow calendar overlap; verify focused assertions in `apps/web/specs/transactionHero.spec.tsx`, `apps/web/specs/homeTransactions.spec.tsx`, and the updated `apps/web/DESIGN.md` contract.
- [x] 10.2 Extend **Deterministic responsive and visual verification** so transaction layouts at 390x844 and 768x1024 in light and dark themes prove the Hero bounding box is 220px tall, its live actions remain above the calendar, and the unchanged overlap exposes more calendar and history content; verify `pnpm test:web`, `pnpm build:web`, and the transaction cases in `pnpm nx e2e web-e2e -- --project=chromium`, then manually compare refreshed transaction baselines with the user-approved 220px option.

## 11. Mobile Transaction Hero Height

- [x] 11.1 Implement **Responsive 200px and 220px transaction hero height** so **The transaction page uses the approved layered banner** at 200px below the 640px `sm` breakpoint and 220px from 640px upward, exposing another 20px of downstream content on mobile while preserving live title/actions, responsive bottom-safe padding, asset fallback, dark-theme treatment, and the 28px/44px normal-flow calendar overlap; verify focused assertions in `apps/web/specs/transactionHero.spec.tsx`, `apps/web/specs/homeTransactions.spec.tsx`, and the updated `apps/web/DESIGN.md` contract.
- [x] 11.2 Extend **Deterministic responsive and visual verification** so transaction layouts at 390x844 and 768x1024 in light and dark themes prove respective 200px and 220px Hero heights, keep live actions fully above the calendar, and preserve the overlap while exposing more mobile calendar and history content; verify `pnpm test:web`, `pnpm build:web`, and the transaction cases in `pnpm nx e2e web-e2e -- --project=chromium`, then manually compare refreshed transaction baselines with the user-approved mobile 200px option.

## 12. Transaction Hero Parallax

- [x] 12.1 Implement **Layered transaction-banner parallax** with the existing `react-use` `useScroll()` hook and transaction scroll-container ref so the decorative background moves up to 24px, the travel illustration moves up to 10px, both amplitudes reduce to 60% below `sm`, and all live Hero content plus existing Hero/calendar geometry remain stationary; honor `prefers-reduced-motion: reduce` and preserve failed-image fallback. The user declined new unit tests; verify through TypeScript/lint and manual mobile/wider browser checks.

## 13. Review Follow-up

- [x] 13.1 Record the explicit 32 by 32 CSS pixel week/month expansion-control exception within **The transaction page uses the approved layered banner** and **The interface uses a one-step compact typography and icon scale**, then align `apps/web/specs/transactionCalendarPresentation.spec.tsx` with the retained `size-8` implementation; verify the focused presentation test passes without changing `TransactionCalendar` runtime code.
- [x] 13.2 Align the **Pages use a mobile-first centered content frame** structural assertion with the existing CSS ownership in which `.page-scaffold` supplies the 16px base gutter and `.layout-main .min-h-full` supplies the 24px gutter from 640px upward; verify `apps/web/specs/visualPrimitives.spec.tsx` and the full `pnpm test:web` suite pass without changing runtime styles.
- [x] 13.3 Keep local package-manager and design-session runtime state out of version control by ignoring `/.pnpm-store/` and `/.superpowers/` and removing their generated files from the staged set without deleting local copies; verify `git status --short` contains no entries from either directory and both paths remain present on disk.
